export const HOST = "127.0.0.1";
export const PORT = "8000";

export const SIGNUP_ENDPOINT = `http://${HOST}:${PORT}/api/accounts/signup`;
export const LOGIN_ENDPOINT = `http://${HOST}:${PORT}/api/accounts/login`;
export const LOGOUT_ENDPOINT = `http://${HOST}:${PORT}/api/accounts/logout`;
export const OAUTH_ENDPOINT = `http://${HOST}:${PORT}/api/accounts/oauth`;